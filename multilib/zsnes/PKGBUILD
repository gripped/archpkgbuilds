# Maintainer: Alexander F. Rødseth <xyproto@archlinux.org>
# Contributor: Levente Polyak <anthraxx@archlinux.org>
# Contributor: Sven-Hendrik Haase <svenstaro@archlinux.org>
# Contributor: Frank Tetzel <frank.tetzel@mailbox.tu-dresden.de>
# Contributor: Hugo Doria <hugo@archlinux.org>
# Contributor: Aurelien Foret <orelien@chez.com>

pkgname=zsnes
pkgver=2.2.1
pkgrel=1
pkgdesc='Super Nintendo emulator'
url='https://github.com/xyproto/zsnes'
arch=(x86_64)
license=(GPL-2.0-only)
depends=(hicolor-icon-theme lib32-libpng lib32-opengl-driver lib32-pipewire lib32-sdl3)
makedepends=(git mesa nasm python)
source=("git+$url#tag=$pkgver")
b2sums=('3003845cbce6e6fce3c73b1b58d6d1c912e1faa795562d627ef6905f3d66618462a162ca36576d4f58159915ac72222ab419066874f1a3e989da42a1c13ef52e')

build() {
  # ZSNES is an old 32-bit x86 binary with hand-written Assembly
  # that does not work well with several common flags.
  unset CFLAGS CXXFLAGS LDFLAGS

  export PKG_CONFIG_PATH=/usr/lib32/pkgconfig
  make -C $pkgname
}

package() {
  DESTDIR="$pkgdir" make -C $pkgname install
}
