# Maintainer: George Hu <integral@archlinux.org>

pkgname=linux-apfs-rw-dkms
_pkgname=${pkgname%-dkms}
epoch=1
pkgver=0.3.21
pkgrel=1
pkgdesc="Experimental APFS kernel module with Write support (DKMS)"
arch=('any')
url="https://github.com/linux-apfs/linux-apfs-rw"
license=(
	'GPL-2.0-only'
	'BSD-3-Clause'
)
depends=(
	'bash'
	'dkms'
)
source=("${pkgname}-${pkgver}.tar.gz::${url}/archive/refs/tags/v${pkgver}.tar.gz")
sha256sums=('7c2af6a0e7f9d0aa2b1dd9dd8e4a03b73f1e5ce429cfa0115e66099c75bd233d')
b2sums=('069bc92a12d583581051e7884e27e137c879629d1547a571e7426e9d6a84a2f73ff590349b0b7e70e7a2326f93040ee9acfe6cbb99595303b9f66cafaafcb53e')

prepare() {
	# Extract BSD-3-Clause terms from sources
	sed -n '2,21p' "${_pkgname}-${pkgver}/lzfse/lzfse.h" > BSD-3-Clause.txt
}

package() {
	local _dkms_dir="${pkgdir}/usr/src/${_pkgname}-${pkgver}/"
	install -d "${_dkms_dir}"
	cp -r ${_pkgname}-${pkgver}/* "${_dkms_dir}"

	install -vDm 644 BSD-3-Clause.txt -t "${pkgdir}/usr/share/licenses/${pkgname}/"
}
