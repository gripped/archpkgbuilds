# Maintainer: George Rawlinson <grawlinson@archlinux.org>
# Contributor: Snowstorm64

pkgname=librashader
pkgver=0.12.0
pkgrel=1
pkgdesc="A preprocessor, compiler, and runtime for RetroArch 'slang' shaders"
arch=(x86_64)
url='https://github.com/SnowflakePowered/librashader'
license=(GPL-3.0-only MPL-2.0)
depends=(
  glibc
  libgcc
  libgl
  libstdc++
  vulkan-driver
  vulkan-icd-loader
)
makedepends=(
  git
  rust
  ninja
)
options=(!lto)
source=("$pkgname::git+$url#tag=librashader-v$pkgver")
sha512sums=('650f0a4d9ea0d63731992b1c5ec67ab5bf1920922e1921fcfc78c0218e23048e246944328891af0baa0d10027fca4469327f6d1fe4ad1e0e14da3a10207e34bd')
b2sums=('c85520b3dd833a82035d76e542c72f19de79064830737ce925bdd171be78be4879d51f115538e92ced0af704fbc7d3165318240a7af7d63de2e3ac25d453094c')

prepare() {
  cd "$pkgname"

  # download dependencies
  cargo fetch --locked --target host-tuple
}

build() {
  cd "$pkgname"

  cargo run \
    -p librashader-build-script \
    -- \
    --stable \
    --profile optimized \
    -- \
    --no-default-features \
    --features=runtime-vulkan,runtime-opengl
}

package() {
  cd "$pkgname"

  # library
  install -vDm755 target/optimized/librashader.so "$pkgdir/usr/lib/librashader.so.${pkgver}"
  ln -rs "$pkgdir/usr/lib/librashader.so.${pkgver}" "$pkgdir/usr/lib/librashader.so"

  # headers
  install -vDm644 -t "$pkgdir/usr/include/librashader" include/*.h

  # license
  install -vDm644 -t "$pkgdir/usr/share/licenses/$pkgname" LICENSE*
}
