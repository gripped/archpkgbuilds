# Maintainer: Jakub Klinkovský <lahwaacz at archlinux dot org>

_name=nvidia-ml-py
pkgname=python-$_name
pkgver=13.595.45
pkgrel=1
pkgdesc='Python bindings for the NVIDIA Management Library (NVML)'
arch=(any)
url='https://pypi.org/project/nvidia-ml-py'
license=(BSD-3-Clause)
depends=(
  nvidia-utils
  python
)
makedepends=(
  python-build
  python-installer
  python-setuptools
  python-wheel
)
source=(https://files.pythonhosted.org/packages/source/${_name::1}/$_name/${_name//-/_}-$pkgver.tar.gz)
b2sums=('124326befb402e3572eb31cbdc13760740b8f60f537201304dd4f5777f990f41f5b95678ed7f5aadfc9813641d029288e552f7201b0405b550b96e970df6179f')

prepare() {
  cd ${_name//-/_}-$pkgver
  # Extract the license which is included at the end of the README.txt file
  grep -A 999999 "COPYRIGHT" README.txt > LICENSE
}

build() {
  cd ${_name//-/_}-$pkgver
  python -m build --wheel --no-isolation
}

package() {
  cd ${_name//-/_}-$pkgver
  python -m installer --destdir="$pkgdir" dist/*.whl

  # Install the license
  install -vDm 644 LICENSE -t "$pkgdir"/usr/share/licenses/$pkgname/

  # Move example.py to correct location
  local site_packages=$(python -c "import site; print(site.getsitepackages()[0])")
  install -vdm 755 "$pkgdir"/usr/share/doc/$pkgname/
  mv -v "$pkgdir$site_packages"/example.py "$pkgdir"/usr/share/doc/$pkgname/
  rm -v "$pkgdir$site_packages"/__pycache__/example.*
}
