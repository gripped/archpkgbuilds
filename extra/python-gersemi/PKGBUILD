# Maintainer: Jakub Klinkovský <lahwaacz at archlinux dot org>

_name=gersemi
pkgname=python-$_name
pkgver=0.28.1
pkgrel=1
pkgdesc="A formatter to make your CMake code the real treasure"
arch=(x86_64)
url="https://github.com/blankspruce/gersemi"
license=(MPL-2.0)
depends=(
  glibc
  libgcc
  python
  python-yaml
  sqlite
)
makedepends=(
  python-build
  python-installer
  python-setuptools
  python-setuptools-rust
)
checkdepends=(
  git
  python-pytest
  python-pytest-xdist
  python-pydantic
  python-lark-parser
)
source=($pkgname-$pkgver.tar.gz::$url/archive/refs/tags/$pkgver.tar.gz)
b2sums=('759ecf7e1607fbd287fda235a0c6ff420551231d1cee818f7fecfe6de2311b227de18d67f3bca74decc8c23321f4f247db1200efe6c14449cc06151312a0779a')

build() {
  cd $_name-$pkgver

  export LIBSQLITE3_SYS_USE_PKG_CONFIG=1

  python -m build --wheel --no-isolation

}

check() {
  local pytest_options=(
    -vv
    -W ignore::DeprecationWarning
    # distribute tests across multiple CPUs
    -n auto
    # failing as of 0.27.2
    --deselect tests/test_configuration.py::test_schema_in_repository_is_consistent_with_configuration_definition
  )

  cd $_name-$pkgver
  python -m venv --system-site-packages test-env
  test-env/bin/python -m installer dist/*.whl
  # the tests use Python subprocess module to execute gersemi, so we must actually activate the venv
  (
    source test-env/bin/activate
    python -m pytest "${pytest_options[@]}" tests
  )
}

package() {
  cd $_name-$pkgver
  python -m installer --destdir="$pkgdir" dist/*.whl
  install -vDm 644 LICENSE -t "$pkgdir"/usr/share/licenses/$pkgname/
}
