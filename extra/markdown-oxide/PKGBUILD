# Maintainer: Caleb Maclennan <caleb@alerque.com>

pkgname=markdown-oxide
pkgver=0.25.11
pkgrel=1
pkgdesc='PKM oriented Markdown Language Server with Obsidian syntax support'
arch=(x86_64)
url="https://github.com/Feel-ix-343/$pkgname"
license=(Apache-2.0)
depends=(gcc-libs
         glibc)
makedepends=(cargo)
_archive="$pkgname-$pkgver"
source=("$url/archive/v$pkgver/$_archive.tar.gz")
sha256sums=('274446b861ebbf3098dcb7e0a5d3135df88df34b16a2c128288f900391dfbf75')

prepare() {
	cd "$_archive"
	cargo fetch --locked --target "$(rustc --print host-tuple)"
}

build() {
	cd "$_archive"
	cargo build --release --frozen
}

package() {
	cd "$_archive"
	install -Dm0755 -t "$pkgdir/usr/bin/" "target/release/$pkgname"
	install -Dm0644 -t "$pkgdir/usr/share/licenses/$pkgname/" LICENSE
}

