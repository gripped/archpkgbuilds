# Maintainer: Jan Alexander Steffens (heftig) <heftig@archlinux.org>

pkgname=valijson
pkgver=1.1.1
pkgrel=1
pkgdesc="Header-only JSON Schema validation library for C++"
url="https://github.com/tristanpenman/valijson"
arch=(any)
license=(BSD-2-Clause)
depends=(
  boost
)
makedepends=(
  cmake
  git
  ninja
  qt5-base
)
source=("git+$url#tag=v$pkgver")
b2sums=('cec4bf5b9bd4a8abd8cce01ad74b39dca7f88b93bb54653b92d41dd4cae04113ef9af8c33bf330c29e0eafb8ccad47a023794c205b4145a562bf77fda0688152')

build() {
  local cmake_options=(
    -D CMAKE_BUILD_TYPE=Release
    -D CMAKE_INSTALL_PREFIX=/usr
  )

  cmake -S valijson -B build -G Ninja "${cmake_options[@]}"
  cmake --build build
}

check() {
  ctest --test-dir build --output-on-failure --stop-on-failure
}

package() {
  DESTDIR="$pkgdir" cmake --install build

  install -Dm644 valijson/LICENSE -t "$pkgdir/usr/share/licenses/$pkgname"
}

# vim:set sw=2 sts=-1 et:
