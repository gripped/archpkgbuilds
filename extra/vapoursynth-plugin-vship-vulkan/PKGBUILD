# Maintainer: Maxime Gauduin <alucryd@archlinux.org>
# Contributor: CreamSodass <greatvaluecreamsoda@gmail.com>

pkgname=vapoursynth-plugin-vship-vulkan
pkgrel=1
pkgver=5.0.2
pkgdesc='Vship Vapoursynth Plugin'
arch=(x86_64)
url=https://codeberg.org/Line-fr/Vship
license=(MIT)
depends=(
  glibc
  libgcc
  libstdc++
  vapoursynth
  vulkan-icd-loader
)
makedepends=(
  clang
  git
  vulkan-headers
)
provides=(vapoursynth-plugin-vship)
conflicts=(vapoursynth-plugin-vship)
source=(git+https://codeberg.org/Line-fr/Vship.git#tag=v${pkgver})
b2sums=('956947f48bf86e6f011b24eb7f12d2bcf78715e1a81eafe311357e41e1bf34c70cf478d3c2fcbb428e04fbe0b0060785bbeb84979e354423f9918c1e83c1a2ae')

build() {
  cd Vship
  make buildVulkan
}

package(){
  cd Vship
  install -Dm 755 libvship.so -t "${pkgdir}"/$(python -c 'import vapoursynth;print(vapoursynth.get_plugin_dir())')/
  install -Dm 644 LICENSE -t "${pkgdir}"/usr/share/licenses/${pkgname}/
}

# vim: ts=2 sw=2 et:
