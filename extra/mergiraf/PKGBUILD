# Maintainer: Sven-Hendrik Haase <svenstaro@archlinux.org>
# Maintainer: Campbell Jones <serebit at archlinux dot org>
# Contributor: VCalV

pkgname=mergiraf
pkgver=0.19.1
pkgrel=1
pkgdesc='A syntax-aware git merge driver for a growing collection of programming languages and file formats'
arch=(x86_64)
url='https://mergiraf.org/'
license=(GPL-3.0-or-later)
makedepends=(git rust)
checkdepends=(jujutsu)
options=(!lto)
source=("$pkgname::git+https://codeberg.org/mergiraf/mergiraf#tag=v$pkgver")
sha512sums=('c55bc22cffbe5f0098b564b1d7f6166b043841506ccfc1e817824c87129cbf450b3b61f8832d7bae30867534fa53b4fa019d92861cd273b517b2bb3b6920a117')
b2sums=('d8edbc8742db9109c6e74bb21b069df2ff544cf6a62fe6400e9f280bc7e940308e975b6adca403ac12f61eb3dbb1492080a977f802fa8401fedf5da295b94636')

prepare() {
  cd "$pkgname"

  # download dependencies
  cargo fetch --locked --target host-tuple
}

build() {
  cd "$pkgname"

  cargo build --frozen --release --all-features --bin mergiraf
}

check() {
  cd "$pkgname"

  # See https://codeberg.org/mergiraf/mergiraf/issues/761
  export CFLAGS="${CFLAGS/-O2}"

  cargo test --all-features
}


package() {
  depends+=(
    glibc
    libgcc libgcc_s.so
  )

  cd "$pkgname"

  install -Dm755 -t "$pkgdir/usr/bin" target/release/mergiraf
  
  # documentation
  install -vDm644 -t "$pkgdir/usr/share/doc/$pkgname" README.md

  # license
  install -vDm644 -t "$pkgdir/usr/share/licenses/$pkgname" LICENSE.txt
}
