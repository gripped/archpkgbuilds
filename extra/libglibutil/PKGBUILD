# Maintainer: George Hu <integral@archlinux.org>
# Contributor: Danct12 <danct12@disroot.org>

pkgname=libglibutil
pkgver=1.0.81
pkgrel=1
pkgdesc="Library of glib utilities"
arch=('x86_64')
url="https://github.com/sailfishos/${pkgname}"
license=('BSD-3-Clause')
depends=('glib2')
source=("${pkgname}-${pkgver}.tar.gz::${url}/archive/refs/tags/${pkgver}.tar.gz")
sha512sums=('65e04f53d5f28618dd6147df40f3bc5cd27e1b0f1644304db24a42fefc2c2a41ffcc87a52e640326525ec5f0bb23fe00e3f54a3796cfbe9cf7697931cb5c1d06')

build() {
	KEEP_SYMBOLS=1 make -C "${pkgname}-${pkgver}" release pkgconfig
}

check() {
	make -C "${pkgname}-${pkgver}" test
}

package() {
	cd "${pkgname}-${pkgver}/"
	DESTDIR="${pkgdir}" make install-dev
	install -Dm644 LICENSE -t "${pkgdir}/usr/share/licenses/${pkgname}/"
}
