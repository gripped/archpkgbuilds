# Maintainer: Peter Jung <ptr1337@archlinux.org>
# Contributor: Rafael Dominiquini <rafaeldominiquini at gmail dot com>

pkgname=bazaar
pkgver=0.9.4
pkgrel=3
pkgdesc="A new app store for GNOME with focus on flatpaks, particularly Flathub"
arch=('x86_64')
url="https://github.com/kolunmi/bazaar"
license=(GPL-3.0-only)
depends=(
  glibc
  libgcc
  glib2
  gtk4
  glycin-gtk4
  json-glib
  glycin
  cairo
  pango
  graphene
  gtksourceview5
  flatpak
  appstream
  dconf
  md4c
  webkitgtk-6.0
  hicolor-icon-theme
  libadwaita
  libdex
  libheif
  libxmlb
  libyaml
  libsoup3
  libsecret
  libproxy
  libmalcontent
  malcontent
)
makedepends=(
  meson
  ninja
  gcc
  pkgconf
  git
  glib2-devel
  gobject-introspection
  python-babel
  blueprint-compiler
  vala
)
optdepends=('krunner-bazaar: krunner integration')

source=(git+https://github.com/kolunmi/bazaar#tag=v${pkgver})
sha256sums=('74aad31514c18c06d93ea3936b7be51e15336c2095732bdd51083026402ee93d')

build() {
  cd "${pkgname}"

  meson setup build \
    --prefix=/usr \
    -Dlibdex:default_library=static \
    -Dlibdex:docs=false \
    -Dlibdex:examples=false \
    -Dlibdex:gdbus=disabled \
    -Dlibdex:introspection=disabled \
    -Dlibdex:pygobject=false \
    -Dlibdex:tests=false \
    -Dlibdex:vapi=false
  ninja -C build
}

package() {
  cd "${pkgname}"

  DESTDIR="${pkgdir}" ninja -C build install
  rm -r "${pkgdir}"/usr/include/libdex-1
  rm "${pkgdir}"/usr/lib/libdex-1.a
  rm "${pkgdir}"/usr/lib/pkgconfig/libdex-1.pc
}
