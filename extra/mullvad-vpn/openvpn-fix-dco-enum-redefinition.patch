diff --git i/src/openvpn/dco_linux.h w/src/openvpn/dco_linux.h
index d28e3658..8110de09 100644
--- i/src/openvpn/dco_linux.h
+++ w/src/openvpn/dco_linux.h
@@ -26,10 +26,11 @@
 
 #include "event.h"
 
-#include "ovpn_dco_linux.h"
-
+/* Include netlink headers first so kernel's IFLA_OVPN_MAX is defined before
+ * ovpn_dco_linux.h, allowing its #ifndef guard to work correctly */
 #include <netlink/socket.h>
 #include <netlink/netlink.h>
+#include "ovpn_dco_linux.h"
 
 typedef enum ovpn_key_slot dco_key_slot_t;
 typedef enum ovpn_cipher_alg dco_cipher_t;
diff --git i/src/openvpn/ovpn_dco_linux.h w/src/openvpn/ovpn_dco_linux.h
index 73e19b59..7c87db63 100644
--- i/src/openvpn/ovpn_dco_linux.h
+++ w/src/openvpn/ovpn_dco_linux.h
@@ -237,6 +237,9 @@ enum ovpn_netlink_packet_attrs {
 	OVPN_PACKET_ATTR_MAX = __OVPN_PACKET_ATTR_AFTER_LAST - 1,
 };
 
+/* These enums are now in linux/if_link.h as of kernel 6.x - only define if
+ * the kernel headers don't provide them */
+#ifndef IFLA_OVPN_MAX
 enum ovpn_ifla_attrs {
 	IFLA_OVPN_UNSPEC = 0,
 	IFLA_OVPN_MODE,
@@ -252,5 +255,6 @@ enum ovpn_mode {
 
 	__OVPN_MODE_AFTER_LAST,
 };
+#endif
 
 #endif /* _UAPI_LINUX_OVPN_DCO_H_ */
