# Maintainer: Jakub Klinkovský <lahwaacz at archlinux dot org>

_name=snapper
pkgname=snapper-fmt
pkgver=0.9.1
pkgrel=1
pkgdesc="Semantic line break formatter for Org, LaTeX, Markdown, and plaintext"
arch=(x86_64)
url="https://github.com/TurtleTech-ehf/snapper"
license=(MIT)
depends=(
  glibc
  libgcc
)
makedepends=(
  cargo
)
source=($pkgname-$pkgver.tar.gz::$url/archive/refs/tags/v$pkgver.tar.gz)
b2sums=('9d0bcf728114ade6e865467c0c87808da72b2f7f98176abb52ab5286d420b18f9e6459ba1456cfa45e52ca7bc4f982bc239e93559a3c5b4f14c670fda33c9ea4')

prepare() {
  cd $_name-$pkgver
  cargo fetch --locked --target "$(rustc --print host-tuple)"
}

build() {
  # fix building with LTO
  CFLAGS+=' -ffat-lto-objects'

  cd $_name-$pkgver
  cargo build --frozen --release
}

check() {
  cd $_name-$pkgver
  cargo test --frozen --release
}

package() {
  cd $_name-$pkgver
  install -vDm 755 target/release/snapper-fmt -t "$pkgdir"/usr/bin/
  install -vDm 755 target/release/snapper-gen-docs -t "$pkgdir"/usr/bin/
  install -vDm 644 LICENSE -t "$pkgdir"/usr/share/licenses/$pkgname/
}
