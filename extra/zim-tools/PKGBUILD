# Maintainer: Justin Kromlinger <hashworks@archlinux.org>
pkgname=zim-tools
pkgver=3.8.0
pkgrel=1
pkgdesc="Various ZIM command line tools"
license=('GPL-3.0-or-later')
arch=('x86_64')
provides=('zimwriterfs')
conflicts=('zimwriterfs')
replaces=('zimwriterfs')
depends=('file' 'libzim>=9.8.1' 'docopt' 'gumbo-parser')
makedepends=('meson' 'mustache')
url='https://github.com/openzim/zim-tools'
source=("${pkgname}-${pkgver}.tar.gz::${url}/archive/${pkgver}.tar.gz"
	# https://github.com/openzim/libzim/pull/936
	"0001-Fix-build-with-ICU-76.patch")
sha256sums=('9681f24b9873691f55807efcb9c3758ad000d2fe70b38ca967dd53495dca1bbe'
            '52c0408b29022e1ab6401a5a96596872746f686940c7dc67185932900cbaca23')

prepare() {
	patch -Np1 -d "${pkgname}-${pkgver}" -i ../0001-Fix-build-with-ICU-76.patch
}

build() {
	cd "${pkgname}-${pkgver}"
	arch-meson build
	ninja -C build
}

package() {
	install -Dm644 "${pkgname}-${pkgver}/LICENSE" "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
	cd "${pkgname}-${pkgver}/build"
	DESTDIR="${pkgdir}" ninja install
}
