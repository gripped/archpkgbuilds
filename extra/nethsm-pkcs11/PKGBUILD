# Maintainer: David Runge <dvzrv@archlinux.org>

pkgname=nethsm-pkcs11
pkgver=2.2.0
pkgrel=1
pkgdesc="PKCS#11 driver for NetHSM"
arch=(x86_64)
url="https://github.com/Nitrokey/nethsm-pkcs11"
license=(Apache-2.0)
depends=(glibc)
makedepends=(rust)
options=(!lto)  # NOTE: as ring is used we can not use LTO
source=(
  $pkgname-$pkgver.tar.gz::$url/archive/refs/tags/v$pkgver.tar.gz
)
sha512sums=('1cb260381ab118f7f72aa5e22ff0b63530d2671f2b6872dbb978d38577012c67a24f78a953cc61e7750cc3e2ee121c6e76cbaa539d43d0480761a99cc49258fb')
b2sums=('a68da681bd7a4de76e108f837e7a24ebf1abc7223fe57450b5091271ab5e440695d428909ce95e2f11ef88e938a2a2de7b7ce7dfe2afcb05b84ac145fde4a664')

prepare() {
  cd $pkgname-$pkgver
  cargo fetch --locked --target "$(rustc --print host-tuple)"
}

build() {
  cd $pkgname-$pkgver
  cargo build --frozen --release --all-features
}

check() {
  cd $pkgname-$pkgver
  # using all features pulls in pkcs11-full-tests which are integration tests
  cargo test --frozen --features=default
}

package() {
  cd $pkgname-$pkgver
  install -vDm 755 target/release/lib${pkgname/-/_}.so -t "$pkgdir/usr/lib/"
  install -vDm 644 {{README,features}.md,p11nethsm*.conf} -t "$pkgdir/usr/share/doc/$pkgname/"
  install -vdm 700 "$pkgdir/etc/nitrokey/"
}
