# Maintainer: Felix Yan <felixonmars@archlinux.org>

pkgname=scmpuff
pkgver=0.6.2
pkgrel=1
pkgdesc="Numeric file shortcuts for common git commands"
arch=('x86_64')
url="https://github.com/mroth/scmpuff"
license=('MIT')
depends=('glibc')
makedepends=('go' 'git')
source=("git+https://github.com/mroth/scmpuff.git#tag=v$pkgver")
sha512sums=('9de04f2da70f6b339f60579d48bc474012e23341b94f4100d394041817ab0c371aea73a3083a09f1d159b06d11d7dce3831cb9944bda5743e596cacd0606e608')

build() {
  cd scmpuff
  export GOFLAGS="-buildmode=pie -trimpath -ldflags=-linkmode=external"
  export CGO_LDFLAGS="${LDFLAGS}"
  export CGO_CFLAGS="${CFLAGS}"
  export CGO_CPPFLAGS="${CPPFLAGS}"
  go build -o scmpuff
}

check() {
  cd scmpuff
  go test ./...
}

package() {
  cd scmpuff
  install -Dm644 LICENSE -t "$pkgdir"/usr/share/licenses/scmpuff/
  install -Dm755 scmpuff -t "$pkgdir"/usr/bin/
}
