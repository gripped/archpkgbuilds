# Maintainer: Alexander F. Rødseth <xyproto@archlinux.org>
# Contributor: Wilhem Barbier <wilhem.b@free.fr>

pkgname=raylib
pkgver=6.0
pkgrel=1
pkgdesc='Simple and easy-to-use game programming library'
arch=(x86_64)
url='https://www.raylib.com'
license=(Zlib)
makedepends=(cmake git libx11 libxcursor libxinerama libxrandr vulkan-headers xorg-server-devel xorg-xinput)
source=("git+https://github.com/raysan5/raylib#tag=$pkgver")
b2sums=('4a6bfc4e88900e600ec10f4b42bbdede3f9ea758cf5925fa7193a304360e3b4b92c5ec52e04a3d33f4b624253ee08be92af8442e9217ab2df23e8fa831a5c395')

build() {
  cmake \
    -B build \
    -D BUILD_EXAMPLES=OFF \
    -D BUILD_SHARED_LIBS=ON \
    -D CMAKE_BUILD_TYPE=Release \
    -D CMAKE_C_FLAGS="$CFLAGS -fPIC -w" \
    -D CMAKE_INSTALL_LIBDIR=lib \
    -D CMAKE_INSTALL_PREFIX=/usr \
    -D OpenGL_GL_PREFERENCE=GLVND \
    -D PLATFORM=Desktop \
    -D USE_EXTERNAL_GLFW=OFF \
    -D WITH_PIC=ON \
    -S $pkgname \
    -W no-dev
  make -C build
}

package() {
  DESTDIR="$pkgdir" make -C build install
  # Include files that are used in the raylib examples
  for f in rcamera rgestures; do
    install -Dm644 $pkgname/src/$f.h "$pkgdir/usr/include/$f.h"
  done
  install -Dm644 $pkgname/LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
}
