diff --git a/cmake/ssl.cmake b/cmake/ssl.cmake
index 68478613..0fe886f4 100644
--- a/cmake/ssl.cmake
+++ b/cmake/ssl.cmake
@@ -151,8 +151,9 @@ MACRO(FIND_OPENSSL_VERSION)
       ${version_part} "${${version_part}}")
   ENDFOREACH()
   SET(OPENSSL_VERSION_MAJOR ${OPENSSL_VERSION_MAJOR} CACHE INTERNAL "" FORCE)
-  IF(OPENSSL_VERSION_MAJOR VERSION_EQUAL 3)
-    # OpenSSL 3
+  IF(OPENSSL_VERSION_MAJOR VERSION_EQUAL 3 OR OPENSSL_VERSION_MAJOR VERSION_GREATER 3)
+    # OpenSSL 3 and newer (e.g. 4.x) expose the version as
+    # OPENSSL_VERSION_MAJOR / OPENSSL_VERSION_MINOR / OPENSSL_VERSION_PATCH.
     SET(OPENSSL_FIX_VERSION "${OPENSSL_VERSION_PATCH}")
   ELSE()
     # Verify version number. Version information looks like:
@@ -818,6 +819,7 @@ MACRO(MYSQL_CHECK_SSL_DLLS)
 
       # Different naming scheme for the matching .dll as of SSL 1.1
       # OpenSSL 3.x Look for libcrypto-3-x64.dll or libcrypto-3.dll
+      # OpenSSL 4.x Look for libcrypto-4-x64.dll or libcrypto-4.dll
       # OpenSSL 1.1 Look for libcrypto-1_1-x64.dll or libcrypto-1_1.dll
       # OpenSSL 1.0 Look for libeay32.dll
       SET(SSL_MSVC_VERSION_SUFFIX)
@@ -827,8 +829,9 @@ MACRO(MYSQL_CHECK_SSL_DLLS)
         SET(SSL_MSVC_VERSION_SUFFIX "-1_1")
         SET(SSL_MSVC_ARCH_SUFFIX "-x64")
       ENDIF()
-      IF(OPENSSL_VERSION_MAJOR VERSION_EQUAL 3)
-        SET(SSL_MSVC_VERSION_SUFFIX "-3")
+      IF(OPENSSL_VERSION_MAJOR VERSION_EQUAL 3 OR
+         OPENSSL_VERSION_MAJOR VERSION_GREATER 3)
+        SET(SSL_MSVC_VERSION_SUFFIX "-${OPENSSL_VERSION_MAJOR}")
         SET(SSL_MSVC_ARCH_SUFFIX "-x64")
       ENDIF()
 
@@ -866,9 +869,10 @@ MACRO(MYSQL_CHECK_SSL_DLLS)
   ENDIF()
 ENDMACRO(MYSQL_CHECK_SSL_DLLS)
 
-# Downgrade OpenSSL 3 deprecation warnings.
+# Downgrade OpenSSL 3+ deprecation warnings.
 MACRO(DOWNGRADE_OPENSSL3_DEPRECATION_WARNINGS)
-  IF(OPENSSL_VERSION_MAJOR VERSION_EQUAL 3)
+  IF(OPENSSL_VERSION_MAJOR VERSION_EQUAL 3 OR
+     OPENSSL_VERSION_MAJOR VERSION_GREATER 3)
     IF(MY_COMPILER_IS_GNU_OR_CLANG)
       ADD_COMPILE_FLAGS(${ARGV}
         COMPILE_FLAGS "-Wno-error=deprecated-declarations")
diff --git a/router/src/router/src/certificate_generator.cc b/router/src/router/src/certificate_generator.cc
index f4426f6d..9ccfa9ae 100644
--- a/router/src/router/src/certificate_generator.cc
+++ b/router/src/router/src/certificate_generator.cc
@@ -254,7 +254,7 @@ stdx::expected<X509Cert, std::error_code> CertificateGenerator::generate_x509(
   }
 
   // Set CN value in subject
-  auto name = X509_get_subject_name(cert.get());
+  auto name = X509_NAME_new();
   if (!name) {
     return stdx::unexpected(make_error_code(cert_errc::cert_set_cn_failed));
   }
@@ -263,15 +263,25 @@ stdx::expected<X509Cert, std::error_code> CertificateGenerator::generate_x509(
           name, "CN", MBSTRING_ASC,
           reinterpret_cast<const unsigned char *>(common_name.c_str()), -1, -1,
           0)) {
+    X509_NAME_free(name);
+    return stdx::unexpected(make_error_code(cert_errc::cert_set_cn_failed));
+  }
+
+  // Set Subject
+  if (!X509_set_subject_name(cert.get(), name)) {
+    X509_NAME_free(name);
     return stdx::unexpected(make_error_code(cert_errc::cert_set_cn_failed));
   }
 
   // Set Issuer
   if (!X509_set_issuer_name(cert.get(),
                             ca_cert ? X509_get_subject_name(ca_cert) : name)) {
+    X509_NAME_free(name);
     return stdx::unexpected(make_error_code(cert_errc::cert_set_issuer_failed));
   }
 
+  X509_NAME_free(name);
+
   // Add X509v3 extensions
   X509V3_CTX v3ctx;
   X509V3_set_ctx(&v3ctx, ca_cert ? ca_cert : cert.get(), cert.get(), nullptr,
diff --git a/router/tests/component/test_bootstrap.cc b/router/tests/component/test_bootstrap.cc
index 278cbeee..eb512cd6 100644
--- a/router/tests/component/test_bootstrap.cc
+++ b/router/tests/component/test_bootstrap.cc
@@ -3458,7 +3458,7 @@ INSTANTIATE_TEST_SUITE_P(
             "version is '" +
                 std::to_string(max_supported_version) + "'."}));
 
-std::string get_x509_name(X509_NAME *name) {
+std::string get_x509_name(const X509_NAME *name) {
   char buffer[256];
   X509_NAME_oneline(name, buffer, sizeof(buffer));
   return std::string(buffer);
diff --git a/router/tests/component/test_rest_api_enable.cc b/router/tests/component/test_rest_api_enable.cc
index 8a450c69..d2ba5fc2 100644
--- a/router/tests/component/test_rest_api_enable.cc
+++ b/router/tests/component/test_rest_api_enable.cc
@@ -348,7 +348,7 @@ class TestRestApiEnable : public RouterComponentBootstrapTest {
     X509_ptr cert(PEM_read_bio_X509_AUX(input.get(), nullptr, nullptr, nullptr),
                   X509_free);
 
-    X509_NAME *subject = X509_get_subject_name(cert.get());
+    const X509_NAME *subject = X509_get_subject_name(cert.get());
     BIO_ptr output_bio(BIO_new(BIO_s_mem()), BIO_free);
     X509_NAME_print_ex(output_bio.get(), subject, 0, 0);
 
diff --git a/sql/auth/sql_authentication.cc b/sql/auth/sql_authentication.cc
index 9c45cde5..5efe1894 100644
--- a/sql/auth/sql_authentication.cc
+++ b/sql/auth/sql_authentication.cc
@@ -5284,7 +5284,7 @@ class X509_gen {
     if (!X509_set_pubkey(x509, pkey)) goto err;
 
     /** Set CN value in subject */
-    name = X509_get_subject_name(x509);
+    name = X509_NAME_new();
     if (!name) goto err;
 
     if (!X509_NAME_add_entry_by_txt(name, "CN", MBSTRING_ASC,
@@ -5292,11 +5292,15 @@ class X509_gen {
                                     0))
       goto err;
 
+    if (!X509_set_subject_name(x509, name)) goto err;
+
     /** Set Issuer */
     if (!X509_set_issuer_name(
             x509, self_sign ? name : X509_get_subject_name(ca_x509)))
       goto err;
 
+    X509_NAME_free(name);
+
     /** Add X509v3 extensions */
     X509V3_set_ctx(&v3ctx, self_sign ? x509 : ca_x509, x509, nullptr, nullptr,
                    0);
@@ -5315,6 +5319,7 @@ class X509_gen {
 
     return x509;
   err:
+    if (name) X509_NAME_free(name);
     if (x509) X509_free(x509);
     return nullptr;
   }
diff --git a/storage/ndb/include/util/NodeCertificate.hpp b/storage/ndb/include/util/NodeCertificate.hpp
index 1e9093cc..457430b7 100644
--- a/storage/ndb/include/util/NodeCertificate.hpp
+++ b/storage/ndb/include/util/NodeCertificate.hpp
@@ -225,7 +225,7 @@ class CertSubject {
   size_t timestamp(time_t, char *m, size_t) const;
   size_t timestamp(char *, size_t) const;
   size_t print_name(char *, size_t) const;
-  bool parse_name(struct X509_name_st *);
+  bool parse_name(const struct X509_name_st *);
   bool parse_name(const struct asn1_string_st *);
 
   /* Member variables */
diff --git a/storage/ndb/src/common/util/NodeCertificate.cpp b/storage/ndb/src/common/util/NodeCertificate.cpp
index 5868bf94..c50bee2a 100644
--- a/storage/ndb/src/common/util/NodeCertificate.cpp
+++ b/storage/ndb/src/common/util/NodeCertificate.cpp
@@ -476,7 +479,11 @@ int SigningRequest::finalise(EVP_PKEY *key) {
   /* Set the subject common name */
   char cn[CN_max_length];
   print_name(cn, CN_max_length);
-  set_common_name(X509_REQ_get_subject_name(m_req), cn);
+  X509_NAME *name = X509_NAME_new();
+  if (name == nullptr) return -10;
+  set_common_name(name, cn);
+  X509_REQ_set_subject_name(m_req, name);
+  X509_NAME_free(name);
 
   /* Set the subject alt names extension */
   if (bound_hostnames()) {
@@ -558,7 +565,7 @@ X509 *SigningRequest::create_unsigned_certificate() const {
   if (!cert) return nullptr;
 
   /* Copy name from csr to cert */
-  X509_NAME *name = X509_REQ_get_subject_name(m_req);
+  const X509_NAME *name = X509_REQ_get_subject_name(m_req);
   if (X509_set_subject_name(cert, name) != 1) return nullptr;
 
   /* Set serial number in x509 */
@@ -578,7 +585,7 @@ X509 *SigningRequest::create_unsigned_certificate() const {
 
 bool SigningRequest::parse_name() {
   if (m_req == nullptr) return false;
-  X509_NAME *name = X509_REQ_get_subject_name(m_req);
+  const X509_NAME *name = X509_REQ_get_subject_name(m_req);
   return CertSubject::parse_name(name);
 }
 
@@ -601,8 +608,10 @@ ASN1_STRING *SerialNumber::random(size_t length) {
 
 int SerialNumber::print(char *buf, int len, const ASN1_STRING *serial) {
   int offset = 0;
-  for (int i = 0; i < serial->length && offset < (len - 4); i++)
-    offset += sprintf(buf + offset, "%02X:", serial->data[i]);
+  int length = ASN1_STRING_length(serial);
+  const unsigned char *data = ASN1_STRING_get0_data(serial);
+  for (int i = 0; i < length && offset < (len - 4); i++)
+    offset += sprintf(buf + offset, "%02X:", data[i]);
   if (offset) buf[offset - 1] = '\0';
   return offset;
 }
@@ -612,8 +621,10 @@ void SerialNumber::free(ASN1_STRING *serial) { ASN1_STRING_free(serial); }
 SerialNumber::HexString::HexString(const ASN1_STRING *serial) {
   buf.append("0x");
   int truncated [[maybe_unused]] = 0;
-  for (int i = 0; i < serial->length; i++)
-    truncated = buf.appendf("%02x", serial->data[i]);
+  int length = ASN1_STRING_length(serial);
+  const unsigned char *data = ASN1_STRING_get0_data(serial);
+  for (int i = 0; i < length; i++)
+    truncated = buf.appendf("%02x", data[i]);
   assert(!truncated);
 }
 
@@ -638,8 +649,12 @@ void Certificate::set_expire_time(X509 *cert, int days) {
 }
 
 int Certificate::set_common_name(X509 *cert, const char *CN) {
-  X509_NAME *name = X509_get_subject_name(cert);
-  return CertSubject::set_common_name(name, CN);
+  X509_NAME *name = X509_NAME_new();
+  if (name == nullptr) return 0;
+  int r = CertSubject::set_common_name(name, CN);
+  if (r) X509_set_subject_name(cert, name);
+  X509_NAME_free(name);
+  return r;
 }
 
 size_t Certificate::get_common_name(X509 *cert, char *buf, size_t len) {
@@ -652,8 +667,10 @@ int Certificate::get_signature_prefix(X509 *cert) {
   const ASN1_BIT_STRING *sig = nullptr;
   const X509_ALGOR *algorithm;
   X509_get0_signature(&sig, &algorithm, cert);
-  if (sig && sig->data)
-    prefix = (sig->data[0] << 16) | (sig->data[1] << 8) | sig->data[2];
+  if (sig && ASN1_STRING_length(sig) >= 3) {
+    const unsigned char *data = ASN1_STRING_get0_data(sig);
+    prefix = (data[0] << 16) | (data[1] << 8) | data[2];
+  }
   return prefix;
 }
 
@@ -766,8 +783,12 @@ static bool initClusterCertAuthority(X509 *cert, const char *ordinal) {
   if (r1 == 0) return false;
 
   /* Set subject name */
-  X509_NAME *name = X509_get_subject_name(cert);
+  X509_NAME *name = X509_NAME_new();
+  if (name == nullptr) return false;
   r1 = X509_NAME_add_entry_by_txt(name, "CN", MBSTRING_ASC, subject, -1, -1, 0);
+  if (r1 == 0) { X509_NAME_free(name); return false; }
+  r1 = X509_set_subject_name(cert, name);
+  X509_NAME_free(name);
   if (r1 == 0) return false;
 
   /* Add extension */
@@ -900,11 +921,12 @@ int CertSubject::bound_hostname(int n, char *buffer, int size) const {
     int name_type;
     if (n < sk_GENERAL_NAME_num(m_bound_hostnames)) {
       GENERAL_NAME *name = sk_GENERAL_NAME_value(m_bound_hostnames, n);
-      ASN1_STRING *str =
-          static_cast<ASN1_STRING *>(GENERAL_NAME_get0_value(name, &name_type));
+      const ASN1_STRING *str =
+          static_cast<const ASN1_STRING *>(GENERAL_NAME_get0_value(name, &name_type));
       if (name_type == GEN_DNS) {
-        if (str->length < size) size = str->length;
-        memcpy(buffer, str->data, size);
+        int str_length = ASN1_STRING_length(str);
+        if (str_length < size) size = str_length;
+        memcpy(buffer, ASN1_STRING_get0_data(str), size);
         buffer[size] = '\0';
         nwritten = size;
       }
@@ -922,11 +944,11 @@ bool CertSubject::bound_localhost() const {
   if (sk_GENERAL_NAME_num(m_bound_hostnames) == 1) {
     int name_type;
     GENERAL_NAME *name = sk_GENERAL_NAME_value(m_bound_hostnames, 0);
-    ASN1_STRING *str =
-        static_cast<ASN1_STRING *>(GENERAL_NAME_get0_value(name, &name_type));
+    const ASN1_STRING *str =
+        static_cast<const ASN1_STRING *>(GENERAL_NAME_get0_value(name, &name_type));
     if (name_type == GEN_DNS) {
-      if ((str->length == 9) &&
-          (strncmp("localhost", (const char *)str->data, 9) == 0))
+      if ((ASN1_STRING_length(str) == 9) &&
+          (strncmp("localhost", (const char *)ASN1_STRING_get0_data(str), 9) == 0))
         return true;
     }
   }
@@ -1000,24 +1022,26 @@ size_t CertSubject::print_name(char *buffer, size_t sz) const {
   return len;
 }
 
-bool CertSubject::parse_name(X509_NAME *name) {
+bool CertSubject::parse_name(const X509_NAME *name) {
   int idx = X509_NAME_get_index_by_NID(name, NID_commonName, -1);
   if (idx < 0) return false;
-  X509_NAME_ENTRY *cn = X509_NAME_get_entry(name, idx);
+  const X509_NAME_ENTRY *cn = X509_NAME_get_entry(name, idx);
   if (cn == nullptr) return false;
-  ASN1_STRING *str = X509_NAME_ENTRY_get_data(cn);
+  const ASN1_STRING *str = X509_NAME_ENTRY_get_data(cn);
   return parse_name(str);
 }
 
 bool CertSubject::parse_name(const ASN1_STRING *str) {
   if (str == nullptr) return false;
-  if (str->length == 0) return false;
+  int length = ASN1_STRING_length(str);
+  if (length == 0) return false;
+  const unsigned char *sdata = ASN1_STRING_get0_data(str);
 
   int p = 0;  // cursor into name
-  auto atEnd = [&]() { return (str->length == p); };
-  auto data = [&]() { return (char *)(str->data) + p; };
+  auto atEnd = [&]() { return (length == p); };
+  auto data = [&]() { return (const char *)(sdata) + p; };
   auto find = [&](const char *a, size_t l) {
-    if (str->length <= p) return false;
+    if (length <= p) return false;
     int r = strncmp(data(), a, l);
     if (r == 0) {
       p += l;
@@ -1358,12 +1382,12 @@ BaseString NodeCertificate::serial_number() const {
 
 bool NodeCertificate::parse_name() {
   if (m_x509 == nullptr) return false;
-  X509_NAME *name = X509_get_subject_name(m_x509);
+  const X509_NAME *name = X509_get_subject_name(m_x509);
   int idx = X509_NAME_get_index_by_NID(name, NID_commonName, -1);
   if (idx < 0) return false;
-  X509_NAME_ENTRY *cn = X509_NAME_get_entry(name, idx);
+  const X509_NAME_ENTRY *cn = X509_NAME_get_entry(name, idx);
   if (cn == nullptr) return false;
-  ASN1_STRING *str = X509_NAME_ENTRY_get_data(cn);
+  const ASN1_STRING *str = X509_NAME_ENTRY_get_data(cn);
   return CertSubject::parse_name(str);
 }
 
diff --git a/storage/ndb/src/common/util/testSecureSocket.cpp b/storage/ndb/src/common/util/testSecureSocket.cpp
index 799f39d1..a2db7dda 100644
--- a/storage/ndb/src/common/util/testSecureSocket.cpp
+++ b/storage/ndb/src/common/util/testSecureSocket.cpp
@@ -220,10 +220,13 @@ TlsService::TlsService(bool sink) : m_sink(sink) {
   X509_set_pubkey(tls_cert, tls_key);
 
   /* Set the names */
-  X509_NAME *name = X509_get_subject_name(tls_cert);
+  X509_NAME *name = X509_NAME_new();
+  assert(name);
   X509_NAME_add_entry_by_txt(name, "CN", MBSTRING_ASC,
                              (const unsigned char *)common_name, -1, -1, 0);
-  X509_set_issuer_name(tls_cert, X509_get_subject_name(tls_cert));
+  X509_set_subject_name(tls_cert, name);
+  X509_set_issuer_name(tls_cert, name);
+  X509_NAME_free(name);
 
   /* Set the expiration date */
   X509_gmtime_adj(X509_getm_notBefore(tls_cert), 0);
