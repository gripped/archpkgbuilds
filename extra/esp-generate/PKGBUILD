# Maintainer: Orhun Parmaksız <orhun@archlinux.org>

pkgname=esp-generate
pkgver=0.5.0
pkgrel=1
pkgdesc="Template generation tool to create no_std applications for Espressif's chips"
arch=('x86_64')
url="https://github.com/esp-rs/esp-generate"
license=('MIT' 'Apache-2.0')
makedepends=('cargo')
source=("$pkgname-$pkgver.tar.gz::$url/archive/v$pkgver.tar.gz")
sha256sums=('7fc91aed6b077ce42c2d9f5a87d261a79b52f3dcdbeed702344e5a8d764f34f8')
options=('!lto')

prepare() {
  cd "$pkgname-$pkgver"
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd "$pkgname-$pkgver"
  cargo build --release --frozen
}

check() {
  cd "$pkgname-$pkgver"
  cargo test --frozen
}

package() {
  cd "$pkgname-$pkgver"
  install -Dm 755 "target/release/$pkgname" -t "$pkgdir/usr/bin"
  install -Dm 644 README.md -t "$pkgdir/usr/share/doc/$pkgname"
  install -Dm 644 LICENSE-MIT -t "$pkgdir/usr/share/licenses/$pkgname"
}

# vim: ts=2 sw=2 et:
