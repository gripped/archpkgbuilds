# Maintainer: George Rawlinson <grawlinson@archlinux.org>
# Maintainer: Caleb Maclennan <caleb@alerque.com>
# Contributor: Clement Tsang (xoronth) <cjhtsang@uwaterloo.ca>

pkgname=bottom
pkgver=0.12.3
pkgrel=2
pkgdesc='A graphical process/system monitor'
arch=(x86_64)
url='https://github.com/ClementTsang/bottom'
license=(MIT)
depends=(glibc # libc.so libm.so
         libgcc libgcc_s.so)
makedepends=(git
             rust)
source=("$pkgname::git+$url#tag=$pkgver")
sha512sums=('6ba81fb711a86db6df60f0472c104e4d4d7f5fc3bbf81d7e7058dc56425aa559b8b7e2a384c21f5f2ccee34ec15dd101405e5bac7edd9286b9c3698fb0950bc9')
b2sums=('ca6b0cdf7be8dd7b22409f9db867dfd19da04d9e84c0bc82b8d0be92259f805250693e7e867e258c545daa555b06272261790688ab6ee0eef692b803ad73cc67')

prepare() {
  cd "$pkgname"

  cargo fetch --locked --target host-tuple
}

build() {
  cd "$pkgname"

  BTM_GENERATE=true cargo build --locked --release --features deploy
}

check() {
  cd "$pkgname"

  CARGO_HUSKY_DONT_INSTALL_HOOKS=true cargo test --locked --features deploy
}

package() {
  cd "$pkgname"

  # binary
  install -vDm755 -t "$pkgdir/usr/bin" target/release/btm

  # license
  install -vDm644 -t "$pkgdir/usr/share/licenses/$pkgname" LICENSE

  # documentation
  install -vDm644 -t "$pkgdir/usr/share/doc/$pkgname" *.md
  cp -vr docs sample_configs "$pkgdir/usr/share/doc/$pkgname"
  rm -f "$pkgdir/usr/share/doc/$pkgname/docs/.gitignore"

  cd target/tmp/bottom

  # man page
  install -vDm644 -t "$pkgdir/usr/share/man/man1" manpage/btm.1

  # completions
  install -Dm644 completion/_btm "$pkgdir/usr/share/zsh/site-functions/_btm"
  install -Dm644 completion/btm.bash "$pkgdir/usr/share/bash-completion/completions/btm"
  install -Dm644 completion/btm.fish "$pkgdir/usr/share/fish/vendor_completions.d/btm.fish"
}

# vim:set ts=2 sw=2 et:
