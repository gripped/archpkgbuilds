# Maintainer: Felix Yan <felixonmars@archlinux.org>

pkgname=baidupcs-go
pkgver=4.0.1
pkgrel=1
pkgdesc="Terminal utility for Baidu Network Disk"
arch=('x86_64')
url="https://github.com/qjfoidnh/BaiduPCS-Go"
license=('Apache-2.0')
depends=('glibc')
makedepends=('go' 'git')
conflicts=("baidupcs")
provides=("baidupcs")
source=("git+https://github.com/qjfoidnh/BaiduPCS-Go.git#tag=v$pkgver")
sha512sums=('9341dfffb67f65c481d2f7fe39baa39e3aac25d5f5826be1c09a0a6b8c68f976d04b85c7d8e80a8d7e6583f8fb4b1e0fef4d8bf3f82b3f161c9d5d64cb20881c')

prepare() {
  cd BaiduPCS-Go
  export GOPATH="${srcdir}"
  go mod download -modcacherw -x
}

build() {
  cd BaiduPCS-Go
  export GOFLAGS="-buildmode=pie -trimpath"
  export CGO_LDFLAGS="${LDFLAGS}"
  export CGO_CFLAGS="${CFLAGS}"
  export CGO_CPPFLAGS="${CPPFLAGS}"
  go build -o baidupcs-go
}

package() {
  cd BaiduPCS-Go
  install -Dm755 baidupcs-go "$pkgdir"/usr/bin/baidupcs
}
