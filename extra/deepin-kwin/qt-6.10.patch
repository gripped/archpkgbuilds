diff --git a/CMakeLists.txt b/CMakeLists.txt
index 50cf07fc3..6a79b9bbd 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -70,6 +70,9 @@ find_package(Qt${QT_MAJOR_VERSION} REQUIRED COMPONENTS
     Widgets
     LinguistTools
 )
+if(Qt${QT_MAJOR_VERSION}_VERSION VERSION_GREATER_EQUAL 6.10)
+  find_package(Qt${QT_MAJOR_VERSION} COMPONENTS CorePrivate GuiPrivate REQUIRED)
+endif()
 
 if (QT_MAJOR_VERSION EQUAL "5")
     find_package(Qt5 ${QT_MIN_VERSION} CONFIG REQUIRED COMPONENTS X11Extras)
