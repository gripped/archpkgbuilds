# Maintainer: Antonio Rojas <arojas@archlinux.org>
# Contributor: Maximilian Stahlberg <maximilian.stahlberg tu-berlin de>

pkgname=python-pyscipopt
pkgver=5.0.1
pkgrel=2
pkgdesc='A Python interface to the SCIP Optimization Suite'
arch=(x86_64)
url='https://www.scipopt.org/'
license=(MIT)
depends=(glibc
         python
         scip)
makedepends=(cython
             git
             python-build
             python-installer
             python-setuptools
             python-wheel)
source=(git+https://github.com/SCIP-Interfaces/PySCIPOpt#tag=v$pkgver)
sha256sums=('73ab422573be1360a9023ae666ea8c209d02a816df6b89dd28e28a89194f2d26')

build() {
  cd PySCIPOpt
  python -m build --wheel --no-isolation
}

package() {
  cd PySCIPOpt
  python -m installer --destdir="$pkgdir" dist/*.whl
  install -Dm644 LICENSE -t "$pkgdir"/usr/share/licenses/$pkgname
}
