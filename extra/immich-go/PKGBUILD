# Maintainer: Sven-Hendrik Haase <svenstaro@archlinux.org>
pkgname=immich-go
pkgver=0.27.0
pkgrel=1
epoch=1
pkgdesc="Unofficial immich-cli alternative"
arch=('x86_64')
url="https://github.com/simulot/immich-go"
license=('AGPL-3.0-only')
makedepends=('go' 'git')
source=("git+https://github.com/simulot/immich-go.git#tag=v$pkgver")
sha512sums=('c96efe11f8fc62f2ee6d890e58be06f5412bb5d7b58a1ddd20763d2cb5cfa181d043dd287b5bfeb28c8b8284ef9e71b3f31e0e1d3872c52fddfa181194201346')

prepare() {
  cd "$pkgname"
  mkdir -p build
}

build() {
  cd "$pkgname"
  export CGO_CPPFLAGS="${CPPFLAGS}"
  export CGO_CFLAGS="${CFLAGS}"
  export CGO_CXXFLAGS="${CXXFLAGS}"
  export CGO_LDFLAGS="${LDFLAGS}"
  export GOFLAGS="-buildmode=pie -trimpath -mod=readonly -modcacherw"
  go build \
    -ldflags "-linkmode=external -X github.com/simulot/immich-go/app.Version=$pkgver-arch -X github.com/simulot/immich-go/app.Commit=$(git rev-parse HEAD)" \
    -o build main.go
}

check() {
  cd "$pkgname"
  go test -v -x ./...
}

package() {
  cd "$pkgname"
  ls -lha build
  install -Dsm755 build/main "$pkgdir/usr/bin/immich-go"
}
