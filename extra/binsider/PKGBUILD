# Maintainer: Orhun Parmaksız <orhun@archlinux.org>

pkgname=binsider
pkgver=0.3.1
pkgrel=1
pkgdesc="A TUI for analyzing binaries like a boss"
arch=('x86_64')
url="https://github.com/orhun/binsider"
license=('MIT' 'Apache-2.0')
depends=('gcc-libs')
makedepends=('cargo')
source=("$pkgname-$pkgver.tar.gz::$url/archive/v$pkgver.tar.gz")
sha512sums=('c058bc00338721274a59ebb2b65c118eb68e9e27f6e22367776751b65679374f1bc65bc46df90560fc13bd09c3a8461af81ab696853688978f68b06390725a69')

prepare() {
  cd "$pkgname-$pkgver"
  cargo fetch --locked --target "$(rustc --print host-tuple)"
}

build() {
  cd "$pkgname-$pkgver"
  cargo build --release --frozen
}

check() {
  cd "$pkgname-$pkgver"
  cargo test --frozen --lib
}

package() {
  cd "$pkgname-$pkgver"
  install -Dm 755 "target/release/$pkgname" -t "$pkgdir/usr/bin"
  install -Dm 644 README.md -t "$pkgdir/usr/share/doc/$pkgname"
  install -Dm 644 LICENSE-MIT -t "$pkgdir/usr/share/licenses/$pkgname"
}

# vim: ts=2 sw=2 et:
