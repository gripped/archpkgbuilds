# Maintainer: kpcyrd <kpcyrd[at]archlinux[dot]org>

pkgname=hussh
pkgver=0.2.0
pkgrel=1
pkgdesc='Memory-safe SSH daemon for restricted TCP forwarding'
url='https://github.com/kpcyrd/hussh'
arch=('x86_64')
license=('GPL-3.0-or-later')
depends=(
  'glibc'
  'libgcc' 'libgcc_s.so'
)
makedepends=(
  'cargo'
  'scdoc'
)
backup=('etc/hussh.conf')
source=(https://github.com/kpcyrd/${pkgname}/archive/v${pkgver}/${pkgname}-${pkgver}.tar.gz)
sha256sums=('fc0937c74a534dce8e8f8a568c6ac7a960fc28cbe8b64973f2d3081bdbcbcf83')
b2sums=('4796aad20af361341bd56897c72069a11944726dc688f422c2159c7b96c428771f5333b6deb0b57ca8a11d055e22209ccfcaa1b0e740a1cc75bd3baa84849fbc')

prepare() {
  cd "${pkgname}-${pkgver}"
  cargo fetch --locked --target host-tuple
}

build() {
  cd "${pkgname}-${pkgver}"
  CFLAGS+=' -ffat-lto-objects'
  cargo build --frozen --release
  make docs
}

check() {
  cd "${pkgname}-${pkgver}"
  cargo test --frozen
}

package() {
  cd "${pkgname}-${pkgver}"
  install -vDm 755 -t "${pkgdir}/usr/bin" \
    target/release/hussh

  # install configs
  install -vDm 644 contrib/hussh.conf -t "${pkgdir}/etc"

  # install systemd configs
  install -vDm 644 -t "${pkgdir}/usr/lib/systemd/system" \
    contrib/hussh.service

  # install docs
  install -vDm 644 README.md -t "${pkgdir}/usr/share/doc/${pkgname}"
  install -vDm 644 -t "${pkgdir}/usr/share/man/man1" \
    contrib/hussh.1
}

# vim: ts=2 sw=2 et:
