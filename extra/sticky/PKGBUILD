# Maintainer: Balló György <ballogyor+arch at gmail dot com>

pkgname=sticky
pkgver=1.30
pkgrel=3
pkgdesc='Sticky notes app for the Linux desktop'
arch=(any)
url='https://github.com/linuxmint/sticky'
license=(GPL-2.0-or-later)
groups=(x-apps)
depends=(
  bash
  dconf
  glib2
  gspell
  gtk3
  hicolor-icon-theme
  pango
  python
  python-gobject
  python-xapp
  xapp
)
makedepends=(
  git
  meson
)
source=("git+https://github.com/linuxmint/sticky.git#tag=$pkgver")
b2sums=(2f794ecfb30e826f16f954b5caf8b8e6ee36907df18a1228e6b1032f4ce9d89db9c29c66b69429040d3655180d468c04b297ecf3e55bd36b01dcf54ffceb93a8)

prepare() {
  cd $pkgname

  # Set window icon
  # https://github.com/linuxmint/sticky/pull/168
  sed -i '/Gtk.Application.do_activate(self)/a\        Gtk.Window.set_default_icon_name("sticky")' usr/lib/sticky/sticky.py

  # Set version number
  sed -i "s/__DEB_VERSION__/$pkgver/" usr/lib/sticky/sticky.py
}

build() {
  arch-meson $pkgname build
  meson compile -C build
}

package() {
  meson install -C build --destdir "$pkgdir"

  python -m compileall -d /usr/lib "$pkgdir/usr/lib"
  python -O -m compileall -d /usr/lib "$pkgdir/usr/lib"
}
