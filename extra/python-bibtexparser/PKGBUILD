# Maintainer: Caleb Maclennan <caleb@alerque.com>
# Contributor: JP-Ellis <josh@jpellis.me>
# Contributor: Francois Boulogne <fboulogne at april dot org>

pkgname=python-bibtexparser
_pyname=${pkgname#python-}
pkgver=2.0.1
pkgrel=1
pkgdesc="Bibtex parser in Python"
arch=(any)
url="https://pypi.org/project/bibtexparser/"
license=(LGPL-3.0-only)
depends=(python
         python-pyparsing)
makedepends=(python-{build,installer,wheel}
             python-setuptools)
# checkdepends=(python-pytest)
_archive="$_pyname-$pkgver"
source=("https://files.pythonhosted.org/packages/source/${_pyname::1}/$_pyname/$_archive.tar.gz")
sha256sums=('672a59f4ba431c4e9a6647cc7c42a267b22e257bcd2cfdb661863f3756c9ab14')

build() {
	cd "$_archive"
	python -m build -wn
}

# Upstream has tests for 2.x currently in beta, but not 1.4
check() {
	cd "$_archive"
	# pytest
}

package() {
	cd "$_archive"
	python -m installer -d "$pkgdir/" dist/*.whl
}
