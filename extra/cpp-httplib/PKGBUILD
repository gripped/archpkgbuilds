# Maintainer: Maxime Gauduin <alucryd@archlinux.org>
# Contributor: sum01 <sum01@protonmail.com>

pkgname=cpp-httplib
pkgver=0.52.0
pkgrel=1
pkgdesc='A C++ header-only HTTP/HTTPS server and client library'
arch=(any)
url=https://github.com/yhirose/cpp-httplib
license=(MIT)
makedepends=(
  brotli
  git
  meson
  ninja
  mbedtls
  openssl
  python
  zlib
  zstd
)
checkdepends=(gtest)
optdepends=(
  'brotli: Brotli compression support'
  'mbedtls: HTTPS support'
  'openssl: HTTPS support'
  'zlib: Zlib compression support'
  'zstd: Zstandard compression support'
)
source=(git+${url}.git#tag=v${pkgver})
b2sums=('09ea2391b5bec9d79ef71b8927aa85caea5b6c93b68f9c678825ca98223e7504cfb07a14e40de12fb3681f3d756dd1fd9d41edab0cab813365caa57bc877b1ba')

build() {
  arch-meson cpp-httplib build
  meson compile -C build
}

#check() {
#  ctest --test-dir build -F
#}

package() {
  DESTDIR="${pkgdir}" meson install -C build
  install -Dm 644 cpp-httplib/LICENSE -t "${pkgdir}"/usr/share/licenses/${pkgname}/
}
