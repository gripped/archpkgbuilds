# Maintainer: David Runge <dvzrv@archlinux.org>

pkgname=openpgp-card-tool-git
pkgver=0.1.7
pkgrel=1
pkgdesc="A simple tool for Git signing and verification with a focus on OpenPGP cards"
arch=(x86_64)
url="https://codeberg.org/openpgp-card/oct-git"
_name="${url##*/}"
license=('Apache-2.0 OR MIT')
depends=(
  glibc
  libgcc
  openssl
  org.freedesktop.secrets
  sqlite
)
makedepends=(
  dbus
  pcsclite
  rust
)
source=(
  $pkgname-$pkgver.tar.gz::$url/archive/v$pkgver.tar.gz
)
sha512sums=('446f982140d9c9db4f2da6c98422c8d72650bd4ce26bcc1a496b1f8620fa8fe262a1d8dd3d93c6bebddb825e7d8478435af35368f49e3db2e7920efcde171d48')
b2sums=('7b9250d69f55102fc9bdb2a2c636fe94fbf67cf30830addd894f463599c6551ae162752464f1afb90e1efd039efc2e303951003571b23f63368c1a26070adb6b')

prepare() {
  cd $_name
  cargo fetch --locked --target "$(rustc --print host-tuple)"
}

build() {
  cd $_name
  cargo build --frozen --release --all-features
}

check() {
  cd $_name
  cargo test --frozen --all-features
}

package() {
  depends+=(
    dbus libdbus-1.so
    pcsclite libpcsclite.so
  )

  cd $_name
  install -vDm 755 -t "$pkgdir/usr/bin/" target/release/oct-git
  install -vDm 644 README.md -t "$pkgdir/usr/lib/systemd/user/"
  install -vDm 644 LICENSES/*.txt -t "$pkgdir/usr/share/licenses/$pkgname/"
}
