# Maintainer: David Runge <dvzrv@archlinux.org>

pkgname=megra
_name=${pkgname}.rs
pkgver=0.0.18
pkgrel=1
pkgdesc="The Mégra Live Coding Language"
arch=(x86_64)
url="https://codeberg.org/parkellipsen/megra.rs"
license=(GPL-3.0-or-later)
depends=(
  alsa-lib
  bzip2
  glibc
  jack  # dlopened
  libgcc
  openssl
  zstd
)
makedepends=(
  rust
)
source=(
  $pkgname-$pkgver.tar.gz::$url/archive/v$pkgver.tar.gz
)
sha512sums=('1271ec7e63a3b6a53e3d0e5b048034924737c13c2eeb548a5c567a00b8d4392cb7b0e888b51444538b53cc3d746e88f90ac64827d76fa0220d1e5c72b0f3aba1')
b2sums=('aee443e9c9af293ddf38dc7547f9cd474e15c7e75489825e2819b89d987b45cb6aeb8c93f3c90b53665aa3674f78b9b6d24da184a99ce8aa2c775626233b6372')

prepare() {
  cd $_name
  cargo fetch --locked --target host-tuple
}

build() {
  cd $_name
  export ZSTD_SYS_USE_PKG_CONFIG=1
  CFLAGS+=" -ffat-lto-objects"
  cargo build --frozen --release --features low_latency
}

check() {
  cd $_name
  cargo test --frozen
}

package() {
  cd $_name
  install -vDm 755 target/release/${pkgname}_rs -t "$pkgdir/usr/bin/"
  install -vDm 644 helper_functions/* -t "$pkgdir/usr/share/doc/$pkgname/helper_functions/"
  install -vDm 644 README.md -t "$pkgdir/usr/share/doc/$pkgname/"
  install -vDm 644 changelogs/*.md "$pkgdir/usr/share/doc/$pkgname/"
}
