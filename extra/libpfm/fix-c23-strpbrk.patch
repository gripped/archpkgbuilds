Description: Preserve const qualifiers returned by C23 string searches.
 The event delimiter check only needs to test whether a match exists;
 the test helper only reads the matching PMU separator.
--- a/lib/pfmlib_common.c
+++ b/lib/pfmlib_common.c
@@ -1893,8 +1893,7 @@ pfmlib_parse_event(const char *event, pfmlib_event_desc_t *d)
 	/*
 	 * support only one event at a time.
 	 */
-	p = strpbrk(event, PFMLIB_EVENT_DELIM);
-	if (p)
+	if (strpbrk(event, PFMLIB_EVENT_DELIM))
 		return PFM_ERR_INVAL;
 	/*
 	 * create copy because string is const
--- a/tests/validate_x86.c
+++ b/tests/validate_x86.c
@@ -10522,7 +10522,7 @@ check_pmu_supported(const char *evt)
 {
 	pfm_pmu_info_t info;
-	char *p;
+	const char *p;
 	int ret;
 	pfm_pmu_t i;
 
 	memset(&info, 0, sizeof(info));
