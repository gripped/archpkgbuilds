# Maintainer: Carl Smedstad <carsme@archlinux.org>

pkgname=icoextract
pkgver=0.2.0
pkgrel=4
pkgdesc='Extract icons from Windows PE files (.exe/.dll)'
arch=(any)
url=https://projects.jlu5.com/icoextract.html
license=(MIT)
depends=(
  python
  python-pefile
)
makedepends=(
  git
  python-build
  python-installer
  python-setuptools
  python-wheel
)
checkdepends=(
  imagemagick
  mingw-w64-gcc
  python-pillow
  python-pytest
)
optdepends=('python-pillow: required for the optional thumbnailer')
source=("git+https://github.com/jlu5/icoextract.git#tag=$pkgver?signed")
b2sums=('2ee958754636a0ac685a4cd07dac6eace1277fd0140a13a29040b086d6b1f22432b38ad8750dc28c9699df8baf4e683032e3437520d17b895e009229ff57510c')
validpgpkeys=('D5D568B2D34AB32A337944D22EC3F60DE71C0B9D') # James Lu <james@overdrivenetworks.com>

build() {
  cd $pkgname
  python -m build --wheel --no-isolation
}

check() {
  cd $pkgname
  make -C tests
  pytest
}

package() {
  cd $pkgname
  python -m installer --destdir="$pkgdir" dist/*.whl
  install -vDm644 -t "$pkgdir/usr/share/thumbnailers" exe-thumbnailer.thumbnailer
  install -vDm644 -t "$pkgdir/usr/share/licenses/$pkgname" LICENSE
}
