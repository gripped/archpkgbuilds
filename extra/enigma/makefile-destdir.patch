--- a/Makefile.in
+++ b/Makefile.in
@@ -32,15 +32,18 @@ enigma: $(ENIGMA)
 	$(CC) $(LDFLAGS) -o enigma $(ENIGMA) $(LIBS)
 
 install: enigma
-	$(INSTALL_PROGRAM) enigma $(bindir)/enigma
-	-chown root.games $(bindir)/enigma && chmod 2755 $(bindir)/enigma
+	mkdir -p $(DESTDIR)$(bindir)
+	$(INSTALL_PROGRAM) enigma $(DESTDIR)$(bindir)/enigma
+	-chown root:games $(DESTDIR)$(bindir)/enigma && chmod 0755 $(DESTDIR)$(bindir)/enigma
 	#$(INSTALL_DATA) $(srcdir)/enigma.1 $(mandir)/man1/enigma.1
-	mkdir -p $(datadir)/enigma
+	mkdir -p $(DESTDIR)$(datadir)/enigma
 	for i in $(srcdir)/levels/*.set $(srcdir)/levels/*.level; do \
-		$(INSTALL_DATA) $$i $(datadir)/enigma; \
+		$(INSTALL_DATA) $$i $(DESTDIR)$(datadir)/enigma; \
 	done
-	mkdir -p $(localstatedir)/enigma
-	-chown root.games $(localstatedir)/enigma && chmod 2770 $(localstatedir)/enigma
+	mkdir -p $(DESTDIR)$(localstatedir)
+	chmod 0775 $(DESTDIR)$(localstatedir)
+	mkdir -p $(DESTDIR)$(localstatedir)/enigma
+	-chown root:games $(DESTDIR)$(localstatedir)/enigma && chmod 0770 $(DESTDIR)$(localstatedir)/enigma
 
 clean:
 	rm -f *.o enigma
