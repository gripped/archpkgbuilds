# Maintainer: Daniel M. Capella <polyzen@archlinux.org>
# Maintainer: Caleb Maclennan <caleb@alerque.com>

pkgname=selene
pkgver=0.31.0
pkgrel=1
pkgdesc='Blazing-fast modern Lua linter written in Rust'
arch=(x86_64)
url=https://github.com/Kampfkarren/selene
license=(MPL-2.0)
depends=(
  libgcc
  glibc
  openssl
)
makedepends=(
  cargo
  git
)
options=(!lto)
source=("git+$url.git#tag=$pkgver")
b2sums=('7a3d0166e554eb126b8e821a0c5be489592b5ddde1f4dc7fb38028971f8bbaddfab90725c28e2fe8a72feb5d769df10dc7b6e44df12642fa75b057bb67d83b89')

prepare() {
  cd $pkgname
  cargo fetch --locked --target host-tuple
}

build() {
  cd $pkgname
  cargo build --release --locked --offline
}

check() {
  cd $pkgname
  local skipped=(
    # sometimes stack overflows
    lint_filtering::tests::test_lint_filtering
  )
  cargo test --locked --offline -- ${skipped[@]/#/--skip }
}

package() {
  cd $pkgname
  install -Dt "$pkgdir"/usr/bin target/release/$pkgname
}

# vim:set ts=2 sw=2 et:
