# Maintainer: Orhun Parmaksız <orhun@archlinux.org>
# Maintainer: Carl Smedstad <carsme@archlinux.org>

pkgname=jnv
pkgver=0.7.0
pkgrel=1
pkgdesc="Interactive JSON filter using jq"
arch=(x86_64)
url="https://github.com/ynqa/jnv"
license=(MIT)
depends=(
  glibc
  libgcc
)
makedepends=(
  cargo
  clang
)
options=(!lto)
source=("$url/archive/v$pkgver/$pkgname-$pkgver.tar.gz")
b2sums=('5e25bb3677276b87c271c0db8f33ca943d9ee9ca8c5e83137da97a729603058bf6713600cdeff313ebd1084336bf3c273c2054e2b9d4e0273be449cb94759753')

prepare() {
  cd $pkgname-$pkgver
  cargo fetch --locked --target "$(rustc --print host-tuple)"
}

build() {
  cd $pkgname-$pkgver
  cargo build --frozen --release
}

check() {
  cd $pkgname-$pkgver
  cargo test --frozen
}

package() {
  cd $pkgname-$pkgver
  install -vDm755 -t "$pkgdir/usr/bin" target/release/$pkgname
  install -vDm644 -t "$pkgdir/usr/share/licenses/$pkgname" LICENSE
}
