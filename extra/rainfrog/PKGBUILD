# Maintainer: Orhun Parmaksız <orhun@archlinux.org>

pkgname=rainfrog
pkgver=0.3.19
pkgrel=2
pkgdesc="A database management TUI for Postgres"
arch=('x86_64')
url="https://github.com/achristmascarl/rainfrog"
license=('MIT')
depends=('duckdb' 'libgcc' 'sqlite')
makedepends=('cargo' 'clang')
source=("$pkgname-$pkgver.tar.gz::$url/archive/v$pkgver.tar.gz")
sha512sums=('2dc285cb30c7a5c124852257eb025d6a1fb71260330daa7eb203bd3dee8c19617f0fca0ad8023376260faa7c3571b635c031ea84fa097f0fdb8082bdda4dcc8f')
options=('!lto')

prepare() {
  cd "$pkgname-$pkgver"
  sed -i '/^duckdb =/s/"bundled"/"buildtime_bindgen"/' Cargo.toml
  cargo fetch --target "$(rustc --print host-tuple)"
}

build() {
  cd "$pkgname-$pkgver"
  LIBSQLITE3_SYS_USE_PKG_CONFIG=1 cargo build --release --frozen
}

check() {
  cd "$pkgname-$pkgver"
  LIBSQLITE3_SYS_USE_PKG_CONFIG=1 cargo test --frozen
}

package() {
  cd "$pkgname-$pkgver"
  install -Dm 755 "target/release/$pkgname" -t "$pkgdir/usr/bin"
  install -Dm 644 README.md -t "$pkgdir/usr/share/doc/$pkgname"
  install -Dm 644 LICENSE -t "$pkgdir/usr/share/licenses/$pkgname"
}

# vim:set ts=2 sw=2 et:
