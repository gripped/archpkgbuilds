# Maintainer: Balló György <ballogyor+arch at gmail dot com>

pkgname=deja-dup
pkgver=50.0
pkgrel=1
pkgdesc='Easy to use backup tool based on Restic'
arch=(x86_64)
url='https://apps.gnome.org/DejaDup/'
license=(GPL-3.0-or-later)
groups=(gnome-circle)
depends=(
  bash
  dconf
  duplicity
  fuse3
  glib2
  glibc
  gtk4
  gvfs
  hicolor-icon-theme
  json-glib
  libadwaita
  libsecret
  libsoup3
  pango
  restic
)
makedepends=(
  appstream
  blueprint-compiler
  git
  itstool
  meson
  vala
)
checkdepends=(xdg-user-dirs)
optdepends=('rclone: Backup into Google Drive or Microsoft OneDrive')
source=("git+https://gitlab.gnome.org/World/$pkgname.git#tag=$pkgver")
b2sums=(db04f5daf3c9ccc44bd363625cc5339ca3a4088361ec57b40ed2623e0018c3af08e779ce78bbd40af6748a3fcc04647d16f47d57badf45af2ba9fa35151a7fa1)
validpgpkeys=(A3A5C2FC56AE7341D308D8571B50ECA373F3F233) # Michael Terry <mike@mterry.name>

build() {
  arch-meson $pkgname build \
    -D packagekit=disabled
  meson compile -C build
}

check() {
  dbus-run-session \
    meson test -C build --print-errorlogs
}

package() {
  meson install -C build --destdir "$pkgdir"
}
