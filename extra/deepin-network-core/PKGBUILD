# Maintainer: Felix Yan <felixonmars@archlinux.org>

pkgname=deepin-network-core
pkgver=2.0.44
pkgrel=1
pkgdesc='DDE network library and plugins'
arch=('x86_64')
url="https://github.com/linuxdeepin/dde-network-core"
license=('GPL-3.0-or-later' 'LGPL-3.0-or-later')
depends=(deepin-control-center glibc libgcc libstdc++ dtk6core dtk6declarative dtk6log qt6-base qt6-declarative networkmanager-qt systemd-libs)
makedepends=(git cmake deepin-session-shell gtest ninja qt6-tools)
groups=(deepin)
source=(git+https://github.com/linuxdeepin/dde-network-core.git#tag=$pkgver)
sha512sums=('2825648c9b8b8501b56acaea4ac35ed974f576968ef668a84419dfd6d7d2106d50dc67faf40987babae95087f068bb257994cb43ab5c20590c3f99010ac6ac9a')

prepare() {
  cd dde-network-core
  # tests are still Qt5-only upstream; only build them for debug builds
  sed -Ei 's@^add_subdirectory\("tests"\)$@if (CMAKE_BUILD_TYPE STREQUAL "Debug")\n    add_subdirectory("tests")\nendif()@' CMakeLists.txt
  # several legacy subdirs still require unavailable Qt5/KF5 components on Arch
  sed -Ei '/src-old|dcc-network-plugin|example|network-service-plugin|dss-network-plugin|dss_example/d' CMakeLists.txt
}

build() {
  cd dde-network-core
  cmake -GNinja .
  ninja
}

package() {
  cd dde-network-core
  DESTDIR="$pkgdir" ninja install
}
